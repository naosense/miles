name: Sync running data

on:
  workflow_dispatch:
    inputs:
      dt:
        description: 'datetime, format 2021-01-02 12:22:45'
        required: false
      distance:
        description: 'running distance, 5.12'
        required: false

env:
  # change env here
  GITHUB_NAME: naosense
  GITHUB_EMAIL: pingao777@gmail.com

jobs:
  sync:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11.4
      - name: Install dependencies
        run: |
          sudo apt install python3-xkcd fonts-humor-sans
          python -m pip install --upgrade pip
          pip install matplotlib
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          rm ~/.matplotlib/fontList.cache ~/.cache/matplotlib/fontList.json
      - name: Generate new SVG file
        run: |
          python main.py $'${{ github.event.inputs.dt }}' $'${{ github.event.inputs.distance }}'

      - name: Commit and push new files
        run: |
          git config --local user.email "${{ env.GITHUB_EMAIL }}"
          git config --local user.name "${{ env.GITHUB_NAME }}"
          git commit -a -m 'sync running data' || echo "nothing to commit"
          git push || echo "nothing to push"
